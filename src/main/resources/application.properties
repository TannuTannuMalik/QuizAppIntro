spring.application.name=quiztourbackend
# Server port
server.port=8082

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/quiz_app
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Mail configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=luxeliving071@gmail.com
spring.mail.password=wlndkwzyfimzngle  # <-- Ensure this is correct and has no spaces
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true

# Enable STARTTLS
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true  # Ensure that STARTTLS is required

# Optional timeout configurations
spring.mail.properties.mail.smtp.connectiontimeout=5000  # Connection timeout
spring.mail.properties.mail.smtp.timeout=5000  # Read timeout
spring.mail.properties.mail.smtp.writetimeout=5000  # Write timeout

# UTF-8 encoding configuration (recommended for special characters in emails)
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true
